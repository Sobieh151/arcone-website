// Scroll-linked (GSAP ScrollTrigger + `scrub`) motion values — the one
// piece of motion on the site that's tied to scroll position rather than
// viewport-entry, used for the hero background's depth effect as the
// user scrolls past it.

export const heroParallax = {
  scaleTo: 1.25,
  opacityTo: 0.35,
  yPercentTo: 12,
};
